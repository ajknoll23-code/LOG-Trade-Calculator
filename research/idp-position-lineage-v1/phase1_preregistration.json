{
  "study_id": "idp-position-lineage-v1",
  "phase": 1,
  "status": "FROZEN_PRE_EVALUATION",
  "scope": "RESEARCH_ONLY_NO_PRODUCTION_MUTATION",
  "hypothesis": "Migrating the 46 frozen IDP V1 legacy-model-position mismatches to their frozen current valuation positions removes a known classification inconsistency without changing projection methodology, durability methodology, replacement depth, or unrelated players.",
  "frozen_sources": [
    "model/releases/idp-v1/idp_v1_release_manifest.json",
    "model/releases/idp-v1/idp_v1_model_delta_transport_candidate.json",
    "model/releases/idp-v1/production_history_components.json"
  ],
  "target_cohort": "exact frozen rows where legacy_model_position != current_valuation_position",
  "expected_target_count": 46,
  "current_scope_policy": "all 46 remain frozen audit targets; only targets currently present in the production-parity core valuation universe may become migration candidates; free-agent-only or otherwise non-rendered targets are explicit holds",
  "target_position_authority": "frozen IDP V1 current_valuation_position, verified against current generated player_positions for all 46 and against the live merged PLAYER_DB for current-core targets",
  "held_fixed": [
    "2025 true PPG inputs",
    "IDP V1 2026 projection",
    "history weight 0.45",
    "projection weight 0.55",
    "replacement rank 32",
    "frozen deployed IDP V1 replacement baselines",
    "prod transform clamp(-0.10 + 0.75 * ratio, 0.15, 1.55)",
    "position weights",
    "age curves",
    "roles",
    "durability methodology",
    "all offense production multipliers",
    "all non-mismatch IDP production multipliers"
  ],
  "candidate_method": [
    "recompute frozen history component using current valuation position and the immutable IDP V1 history constants",
    "keep the frozen IDP V1 forward projection unchanged",
    "evaluate the clean current-position model against the frozen deployed V1 replacement baseline for the current position",
    "subtract the clean legacy-position V1 model multiplier",
    "transport only that position-lineage delta onto the actual deployed raw PROD_MULT value",
    "apply the existing no-history exact-floor rescue migration guard",
    "round candidate raw PROD_MULT to 4 decimals"
  ],
  "explicit_holds": [
    "rows without a comparable frozen V1 projection/model result",
    "rows triggering the exact-floor no-history rescue discontinuity guard"
  ],
  "hard_gates": [
    "immutable IDP V1 release artifact hashes match the release manifest",
    "target mismatch count is exactly 46",
    "current generated player-position map matches frozen current_valuation_position for every target row",
    "live merged PLAYER_DB position matches frozen current_valuation_position for every current-core target row",
    "current deployed raw PROD_MULT matches the frozen deployed IDP V1 candidate for every current-core target row",
    "every target row is classified as recomputed candidate or explicit documented hold",
    "no unguarded exact-floor no-history rescue discontinuity",
    "all simulated candidate values are finite positive integers",
    "raw multiplier direction never causes opposite-direction final FV movement",
    "zero final-value changes outside the 46-row target cohort",
    "zero offense final-value changes",
    "production index.html remains byte-for-byte unchanged"
  ],
  "decision_if_pass_and_any_change": "PASS_IDP_POSITION_LINEAGE_V1_PHASE1_CANDIDATE_FREEZE",
  "decision_if_integrity_failure": "STOP_IDP_POSITION_LINEAGE_V1_PHASE1_INTEGRITY_FAILURE",
  "decision_if_no_material_change": "STOP_IDP_POSITION_LINEAGE_V1_NO_MATERIAL_CANDIDATE",
  "production_change_authorized": false
}
