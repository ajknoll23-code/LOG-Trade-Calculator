name: Production V2 Phase 6 Transform Compression Audit

# Research-only audit of affine floor/ceiling compression.
on:
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: production-v2-phase6-transform-compression-audit
  cancel-in-progress: false

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check out current main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Self-test Phase 6 audit
        run: python3 research/production-v2/production_v2_phase6_transform_compression_audit.py --selftest

      - name: Run Phase 6 audit
        run: python3 research/production-v2/production_v2_phase6_transform_compression_audit.py --write

      - name: Verify exact reproducibility
        run: python3 research/production-v2/production_v2_phase6_transform_compression_audit.py --check

      - name: Print report
        run: cat research/production-v2/production_v2_phase6_transform_compression_audit.md

      - name: Research-only mutation safety check
        shell: bash
        run: |
          allowed="$(printf '%s\n' \
            research/production-v2/production_v2_phase6_transform_compression_audit.json \
            research/production-v2/production_v2_phase6_transform_compression_audit.md)"

          changed="$(git status --porcelain=v1 --untracked-files=all | cut -c4- | sed '/^$/d' | sort -u)"
          unexpected="$(comm -23 <(printf '%s\n' "$changed") <(printf '%s\n' "$allowed" | sort -u))"

          if [ -n "$unexpected" ]; then
            echo "Unexpected files changed by Phase 6:"
            echo "$unexpected"
            exit 1
          fi

          git add \
            research/production-v2/production_v2_phase6_transform_compression_audit.json \
            research/production-v2/production_v2_phase6_transform_compression_audit.md

          git diff --cached --check

          staged="$(git diff --cached --name-only | sort -u)"
          unexpected_staged="$(comm -23 <(printf '%s\n' "$staged") <(printf '%s\n' "$allowed" | sort -u))"
          if [ -n "$unexpected_staged" ]; then
            echo "Unexpected files staged:"
            echo "$unexpected_staged"
            exit 1
          fi

      - name: Commit Phase 6 audit
        shell: bash
        run: |
          git config user.name "production-v2-audit-bot"
          git config user.email "actions@users.noreply.github.com"

          if git diff --cached --quiet; then
            echo "No audit-output changes to commit."
          else
            git commit -m "Audit Production V2 transform compression"
            git push origin main
          fi
